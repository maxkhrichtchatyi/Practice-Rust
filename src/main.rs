mod general;

fn main() {
    // Work with numbers
    general::numbers();

    // Work with chars
    general::chars();

    // Work with booleans
    general::booleans();

    // Work with operators
    general::operators();

    // Work with scopes
    general::scope_and_shadowing();

    // Work with constants
    general::constants();
}
