mod general;

fn main() {
    // Work with numbers
    general::numbers();

    // Work with chars
    general::chars();

    // Work with booleans
    general::booleans();

    // Work with operators
    general::operators();

    // Work with constants
    general::constants();
}
